---
# Collections the playbooks need. Install once:
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: amazon.aws
    version: ">=9.0.0"
  - name: community.general
    version: ">=9.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
